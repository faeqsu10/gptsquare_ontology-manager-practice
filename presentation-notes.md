# 책과 데이터에서 출발한 Ontology Manager 실습

> 공개 공유본: 배치 ID·측정값·일부 수량·경로는 설명용 예시로 바꿨습니다. 예시 값은 원본 행 또는 실제 운영 결과의 증거가 아닙니다. 원본 CSV·책 본문·계정 정보는 포함하지 않습니다.

발표일: 2026-09-09 / 피드백 반영본 / HTML 13장과 대응

## 전체 이야기

책의 Chapter 10에서 온톨로지 설계 원칙을 익혔다. 스터디 데이터 12종을 살펴보면서 불일치와 연결의 한계를 발견했고, 사람이 근거를 확인하고 판단을 남기는 ‘배치 출하 검토’를 유즈케이스로 정했다. 공유 Foundry 환경에 참여했으며, 브랜치에서 AI FDE에 구현을 요청하고 직접 객체·속성·링크·액션을 구성해 보았다.

## 1. Chapter 10 주요 내용

『팔란티어 파운드리, 판단을 설계하라』 Chapter 10의 장별 학습 노트(p.133–149)를 다시 확인했다.

| 주요 내용 | 공유할 핵심 |
| --- | --- |
| 객체·링크·액션 | 판단 대상, 판단 맥락, 판단 결과를 함께 정의한다. |
| ERP·MES·SCM 재매핑 | 시스템 테이블을 그대로 옮기지 않고 판단에 필요한 단위로 재구성한다. |
| 최소 연결 원칙 | 지금 판단과 행동에 기여하는 관계부터 연결한다. |
| 액션과 함수 | 조건·책임·변경 결과를 정하고 함수로 판단 사유를 표현한다. |
| 시간과 권한 | 과거 이슈와 현재 상태를 구분하고 누가 제안·실행할지 정한다. |
| 운영 중 개선 | 현업과 함께 조정하고 각 요소의 존재 이유를 설명한다. |

책의 함수 예시는 검사 결과와 불량률을 종합하고 판단 사유를 반환한다. 책의 임계치는 운영 정책이며 이번 데이터의 확정된 판정 기준으로 옮기지 않았다. 이번 실습은 사람이 검사 결과를 확인하고 검토 상태·사유·판정자·시각을 남기는 범위다. 함수 기반 종합 판정, 역할별 승인, 상태 유효기간과 전체 이력은 후속 확장이다.

### 다음에 더 만들어 볼 기능 — 아직 미구현

- **재검사부터 검토 완료까지 한 건으로 관리:** 불합격 발견 → 재검사 요청 → 결과 확인 → 완료 과정을 묶는다. 이것이 출하 케이스와 재검사 이력이다.
- **시스템이 검토할 이유를 정리:** 여러 검사 결과를 읽어 “점도 합격, 내용량 불합격 → 내용량 재검사 필요”라고 알려준다. 최종 판단은 사람이 한다. 불량률까지 활용하려면 데이터와 계산·판정 기준부터 확인한다.
- **상태와 담당자에 따라 버튼 제한:** 보류 상태에서는 재검토할 수 있고 최종 승인은 지정 담당자만 하도록 정한다.
- **현재 상태와 이전 판단을 함께 기록:** 어제 보류, 오늘 승인이면 현재는 승인으로 보이되 이전 보류 사유와 담당자도 조회한다.

이번에는 검토 결과를 저장했고, 이후에는 재검사·승인·과정 기록까지 관리할 수 있다는 의미다.

## 2. 데이터를 보고 유즈케이스를 정한 배경

생산·공정·품질·설비·출하 데이터 12종의 컬럼, 값, 연결 키를 확인했다. 원본 CSV는 수정하지 않았다.

| 관찰 | 확인 결과 | 설계에 반영한 방향 |
| --- | --- | --- |
| 불량 유형과 수량 불일치 | 유형 없음 + 수량 양수 18/30건 | 임의 보정 없이 의미와 처리 기준 확인 |
| 측정값과 합불 기준 불명확 | 내용량 합격 범위 97.40~101.20, 불합격 범위 97.90~100.80으로 겹침 | 원본 result 유지, 측정값만으로 자동 재판정하지 않음 |
| 검사와 출하 기록 동시 확인 필요 | 검사60건 중 불합격8건, 모두 내용량; 8개 배치에 출하실적 기록 존재 | 배치에서 검사 근거를 확인하고 검토 결과 기록 |
| 시간 지표 불일치 | 기존 점검 기록상 생산 경과시간과 공정시간 합계30건 모두 다름 | 정의·단위를 확인하고 분리해 해석 |
| 데이터 정제·연결 한계 | 일부 한글 손상; 설비·센서에 배치 직접키 없음 | 복원 규칙 기록, 임의 연결 없이 범위 제한 |

### 발표에서 직접 보여줄 원본 발췌

| 배치 ID | 불량 유형 | 불량 수량 |
| --- | --- | ---: |
| DEMO-01-02 | 없음 | 12 |
| DEMO-02-01 | 없음 | 34 |
| DEMO-02-02 | 없음 | 56 |

출처: `08.defect_summary.csv`. 이런 조합이 18/30건이다.

| 배치 ID | 내용량 측정값 | 검사 결과 |
| --- | ---: | --- |
| DEMO-04-01 | 97.40 | 합격 |
| DEMO-01-02 | 98.20 | 불합격 |
| DEMO-02-02 | 98.21 | 합격 |

출처: `07.quality_inspection.csv`. 제품별 규격 등이 다를 수 있으므로 판정 오류로 단정하지 않는다. 판정 기준이 제공되지 않아 측정값만으로 자동 재판정하지 않는다는 설명에 사용한다.

대표 사례 `DEMO-01-02`의 원본 재확인 결과:

- 내용량: 불합격, 측정값98.20.
- 불량요약: 유형 없음, 수량12.
- 출하실적: SHIP-DEMO-001, 출하일2025-01-02.
- 출하 상태: 원본은 한글 바이트 손상, 기존에 문서화한 복원 매핑으로 완료로 해석.

이는 실제 부적합 출하를 입증하지 않는다. 검사 시점·재검사·예외 승인·최종 출하 규칙이 충분히 제공되지 않았다. 발표에서는 ‘검사 결과와 출하 기록을 함께 확인해야 할 사례를 발견했다’고 설명한다.

여기서 정한 유즈케이스는 **배치별 검사 근거와 데이터의 불확실성을 사람이 검토하고, 상태와 판단 이유를 별도로 기록하는 것**이다. 과거 출하 데이터를 이용해 출하 전 검토 상황을 재현한다. 원천 출하 상태를 덮어쓰지 않고 검토용 속성을 둔다. 모든 데이터 불일치를 자동 검출·해결하는 기능을 구현한 것은 아니다.

## 3. 공유 Foundry 환경에서 브랜치를 만들어 실습

공유 환경 참여 → 실습용 브랜치 생성 → 직접 설정·실행.

브랜치에서 데이터를 준비하고, 객체·링크·액션을 설정해 봤다.

발표 멘트: “공유 환경에 참여해 제 브랜치를 만들고 실습했습니다.” 이후 AI FDE 사용 경험으로 넘어간다.

발표 메모용 상세 기록:

- `review-practice`: 검사 조회 → 실제 액션 제출 → 속성 반영 확인.
- `review-practice-repeat`: 새 브랜치 재실습. 최종 실행 완료는 별도 확인 필요.

## 4. AI FDE 사용 경험 — 두 가지 방향

| 방향 | 요청 | 목적 |
| --- | --- | --- |
| AI에게 구현 맡기기 | 데이터·책의 원칙·목표 구조를 주고 실제 자산 생성 요청 | AI가 요구사항을 얼마나 잘 구현하는지 확인 |
| 직접 실습 안내받기 | 클릭 위치·입력값·설명·저장·성공 확인을 단계별로 요청 | 내가 이해하며 직접 설정·실행 |

두 방식은 하나의 필수 진행 순서가 아니라 사용 목적이 다른 활용 방식이다.
사용자가 제공한 `무제 5.md`는 직접 실습 안내용 원문이다.
발표에 넣은 두 전문은 대화 맥락과 이 원문을 바탕으로 다듬은 재구성본이며 실제 과거 입력문의 인용이 아니다.

- 구현 요청: [프롬프트 MD](prompt-build.md)
- 직접 실습 안내: [프롬프트 MD](prompt-tutor.md)

구현 방식에서는 AI가 자산을 만들도록 요청하고, 안내 방식에서는 사용자가 직접 조작한다.
공통으로 유즈케이스, 데이터의 한계, 액션 중심·최소 연결·원천 사실과 사람의 결정 구분을 전달한다.
생성 자산 전체의 누락 검토 요청과 사용 절차 질문은 다음에 해볼 활용 단계로 구분한다.
정확한 최초 생성 자산 목록과 AI/수동 작업 경계는 변경 이력으로 확인해야 한다.

발표 멘트: “AI FDE를 두 가지 방식으로 써봤습니다. 하나는 직접 만들어 보라고 시켜서 구현 결과를 보는 것이고, 다른 하나는 제가 직접 해볼 수 있도록 설명을 받는 것이었습니다.”

## 5. 직접 실습한 내용

| 단계 | 설정 | 이해한 의미 |
| --- | --- | --- |
| Pipeline Builder | inspection_id를 추가한 검사 출력 Dataset | 검사 한 건을 식별할 키 준비 |
| Object Type | 배치출하검토30건, 품질검사60건 | 객체 한 건이 무엇인지 정의 |
| Property | 원천 컬럼 + 검토 상태·사유·판정자·시각 | 원천 사실과 사람의 결정 구분 |
| Link Type | 검사.batch_id를 배치의 기본키로 연결 | 배치에서 관련 검사 조회 |
| Action Type | 출하 검토 기록, Modify 규칙 | 기존 배치의 검토 속성 수정 |
| 입력 조건 | 보류일 때 보류 사유 필수 | 상태에 맞는 입력 요구 |
| 실제 실행 | Insight → Actions → Submit | 객체 속성의 실제 변경 확인 |

판정자는 Current User, 판정 시각은 Timestamp 속성에 Current Time을 연결했다. 검사 ID에 batch_id와 test_item의 조합을 사용했으므로 같은 항목의 재검사가 생기면 회차나 별도 ID가 필요하다.

막히면서 이해한 내용: 타입 생성과 실제 객체 조회는 별도 확인 대상이다. 파라미터를 수정할 속성에 연결해야 하며, 속성 타입에 맞는 값을 설정해야 한다. Test run과 실제 Submit 후 저장 확인을 구분한다.

기존 성공 사례에서 DEMO-01-02에 보류와 ‘[실습] 내용량 검사 불합격으로 재검사 필요’를 제출했고 판정자·시각까지 반영됐다. 실제 물류 차단·Main 반영·전체 이력 관리는 후속 범위다.

## 6. HTML 발표 순서

25~30분을 기준으로 화면당 핵심 문장 하나를 설명한다.

1. 학습 과정 소개
2. Chapter 10의 주요 내용
3. 책의 원칙을 이번 실습에 적용한 범위
4. 스터디 데이터에서 발견한 점
5. 왜 배치 출하 검토를 선택했는가
6. 배치 선택 → 검사 확인 → 판단 기록
7. 공유 Foundry 환경에서 브랜치를 만들어 실습한 경험
8. AI에게 구현을 맡긴 프롬프트
9. 직접 실습 안내를 요청한 프롬프트
10. Pipeline Builder와 객체·링크 구성
11. 직접 만든 액션과 입력 조건
12. 실습 흐름과 설명용 체험
13. 배운 점과 다음 실습

## 근거

- Chapter 10 기존 장별 학습 노트: 기존 장별 학습 노트 — 이번에 재확인, 책 원본 캡처 전체를 다시 대조한 것은 아님.
- `sessions/week-02/README.md`, `labs/data/manifest.md` — 이전 데이터 점검과 복원 규칙.
- `labs/data/raw/cosmetics-mfg-original-12/` — 검사·불량·출하·설비/센서 원본 재검증.
- `sessions/week-03/README.md` — 공유 환경 이동과 설계 준비 기록.
- `practice-summary.md` — 기존 브랜치의 실제 실행 결과.
- 사용자의2026-09-09 피드백 — AI FDE 활용과 직접 재실습 경험.
