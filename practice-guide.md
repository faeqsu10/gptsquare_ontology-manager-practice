# 온톨로지 매니저 처음부터 끝까지 따라하기

> 공개 공유본: 배치 ID·측정값·일부 수량·경로는 설명용 예시로 바꿨습니다. 예시 값은 원본 행 또는 실제 운영 결과의 증거가 아닙니다. 원본 CSV·책 본문·계정 정보는 포함하지 않습니다.

기존 실습 기록 기반 재구성 · 신규 API 이름과 설명은 제안 예시 · 화면 위치는 환경에 따라 다를 수 있음

각 단계의 **설정 → 저장 → 완료 확인**을 끝낸 뒤 다음 단계로 진행한다.

## 00. 준비: 이번 실습에서 만들 것

**들어갈 곳:** 공유 Foundry 환경 → 실습 프로젝트

| 설정 항목 / 순서 | 입력하거나 확인할 값 |
| --- | --- |
| 만들 흐름 | 배치 선택 → 품질검사 확인 → 검토 상태·사유 입력 → 사용자·시각과 함께 저장 |
| 필요한 원본 | batch_master 30행 / quality_inspection 60행. 현재 브랜치에서 실제 행이 조회되는 원본 사용 |
| 결과물 | 검사 출력 Dataset 1개 · Object Type 2개 · Link Type 1개 · Action Type 1개 |
| 필수 기록 | 프로젝트, 브랜치 이름·URL, 원본·출력 Dataset 이름, 두 Object Type과 Action 이름 |
| 예시 이름 사용 규칙 | 이후 영문 API 이름은 새로 만드는 경우의 제안. 기존 자산을 이어 하면 현재 이름을 유지하고 중복 생성하지 않기 |
| 완료 범위 | 검토 결과 저장까지. Main 병합·실제 출하 차단·중복 보류 방지·전체 이력 관리는 포함하지 않음 |

**저장:** 아직 저장 단계 아님. 원본과 작업 위치부터 확인.

**완료 확인:** 어느 브랜치에서 무엇을 만들지 설명할 수 있고 원본 두 개가 조회된다.

**막힐 때 / 추가 설명:** 이 절차는 기존 성공 실습 기록을 순서대로 재구성했다. 현재 UI의 모든 생성 화면을 새로 재실행한 결과는 아니다. 메뉴 위치·자동 생성 이름은 환경에 따라 다를 수 있다. 설명 문구는 복사할 수 있도록 새로 작성한 예시다.

## 01. 브랜치와 원본 데이터를 확인한다

**들어갈 곳:** Ontology Manager → 브랜치 선택 / 프로젝트의 Dataset

| 설정 항목 / 순서 | 입력하거나 확인할 값 |
| --- | --- |
| 브랜치 | 새 실습이면 Create new branch. 예: review-practice-new. 이어 하면 기존 실습 브랜치 선택 |
| 브랜치 설명 예시 | 배치별 품질검사 확인 후 출하 검토 결과를 기록하는 실습 |
| batch_master 확인 | batch_id, manufacture_date, product_code, recipe_id. 30행 / batch_id 고유 30개 / 빈 키 없음 |
| quality_inspection 확인 | batch_id, test_item, result, measured_value. 60행 / 배치당 검사 2건 |
| 원본이 없는 경우 | 프로젝트에 03.batch_master.csv와 07.quality_inspection.csv를 Dataset으로 가져온 후 열 구분·헤더·타입·행 수 확인 |
| 브랜치별 데이터 확인 | 다른 브랜치의 출력 이름만 보고 재사용하지 않기. 현재 브랜치에서 스키마와 실제 행이 보여야 함 |

**저장:** 브랜치 생성 완료 후 이름과 URL 기록. Dataset 가져오기를 했다면 행 조회까지 확인.

**완료 확인:** 배치30행·검사60행이 보이고, 두 데이터의 batch_id가 연결 가능하다.

**막힐 때 / 추가 설명:** 브랜치를 만들었다고 모든 데이터가 복제되는 것은 아니다. 이 가이드는 조회 가능한 원본 Dataset에서 시작한다. 없으면 원본 가져오기를 먼저 끝낸다. 원본의 불일치를 임의로 수정하지 않는다.

## 02. Pipeline Builder에서 검사 식별자를 만든다

**들어갈 곳:** Pipeline Builder → 새 파이프라인 → 검사 원본 추가

| 설정 항목 / 순서 | 입력하거나 확인할 값 |
| --- | --- |
| 입력 | 현재 브랜치에서 조회되는 quality_inspection Dataset |
| 추가할 컬럼 | inspection_id · String |
| 계산 내용 | batch_id + 구분자 :: + test_item. 예: DEMO-01-02::내용량. 문자열 이어붙이기 변환으로 구성 |
| 출력할 컬럼 | inspection_id(String), batch_id(String), test_item(String), result(String), measured_value(Double) |
| 출력 Dataset 이름 예시 | quality_inspection_for_ontology_practice_0909. 이미 검증된 출력이 있으면 재사용 가능 |
| 파이프라인 설명 예시 | 품질검사에 고유 inspection_id를 추가해 객체 생성과 배치 연결에 사용한다 |

**저장:** 출력 Dataset 연결 → 저장 → Deploy/실행 → 빌드 성공 확인.

**완료 확인:** 출력60행 / 검사 ID 고유60개 / 빈 ID0개 / 배치 연결 누락0개. 원천 검사 결과·측정값은 유지된다.

**막힐 때 / 추가 설명:** 위 계산은 의미를 설명한 식이며 UI의 코드 입력 문법을 뜻하지 않는다. Add column 또는 문자열 연결 변환에서 해당 컬럼과 상수를 지정한다. 같은 배치의 같은 항목을 재검사하면 ID가 중복될 수 있으므로 별도 검사 ID나 회차가 필요하다.

## 03. 배치출하검토 Object Type을 만든다

**들어갈 곳:** Ontology Manager → New → Object type → Use existing datasource

| 설정 항목 / 순서 | 입력하거나 확인할 값 |
| --- | --- |
| Display name / 복수 표시 이름 | 배치출하검토 / 배치출하검토 |
| API name 예시 | BatchShipmentReviewPractice0909 · 충돌 없는 새 이름 사용 |
| Description | 배치별 품질검사 근거를 확인하고 출하 검토 상태와 판단 사유를 기록한다. |
| Datasource | 1단계에서 확인한 batch_master |
| Primary key / Title | 둘 다 batch_id. 기본키는 객체 식별용, Title은 목록에 보여줄 대표값 |
| 원천 속성 매핑 | Batch Id ← batch_id / Manufacture Date ← manufacture_date / Product Code ← product_code / Recipe Id ← recipe_id. 기존 실습은 네 속성 모두 String |
| Allow edits / Edits | 배치출하검토의 편집 허용을 켜고 Edits Enabled 상태 확인 |

**저장:** 생성 화면에서 완료 → Save to branch → 변경사항 확인 화면에서 저장.

**완료 확인:** 타입에 원천 속성4개, 기본키와 Title=batch_id, 올바른 데이터 소스가 표시된다.

**막힐 때 / 추가 설명:** 같은 목적의 객체가 이미 있으면 이름만 같다고 새로 만들지 말고 기본키·데이터 소스·속성을 확인해 재사용한다. 기존 날짜 속성 타입이 다르면 원천 형식과 일치하는지 확인한다. Pipeline Builder가 관리하는 객체가 편집 불가로 표시되면 생성 위치와 편집 범위를 먼저 확인한다.

## 04. 품질검사 Object Type을 만든다

**들어갈 곳:** Ontology Manager → New → Object type → Use existing datasource

| 설정 항목 / 순서 | 입력하거나 확인할 값 |
| --- | --- |
| Display name / 복수 표시 이름 | 품질검사 / 품질검사 |
| API name 예시 | QualityInspectionPractice0909 · 기존 이름과 충돌하지 않게 지정 |
| Description | 배치별 검사 항목, 측정값, 합격·불합격 결과를 조회한다. |
| Datasource | 2단계에서 빌드 성공한 검사 출력 Dataset. inspection_id가 있는 출력 선택 |
| Primary key / Title | 둘 다 inspection_id. batch_id는 같은 배치에 검사2건이 있어 기본키로 사용하지 않음 |
| 속성 매핑과 타입 | Inspection Id ← inspection_id(String), Batch Id ← batch_id(String), Test Item ← test_item(String), Result ← result(String), Measured Value ← measured_value(Double) |
| 검사 편집 | 이번 액션은 검사 결과를 수정하지 않음. 검사값은 원본 결과를 유지 |

**저장:** 생성 완료 → Save to branch. 배치출하검토와 같은 브랜치인지 확인.

**완료 확인:** 속성5개, 기본키=inspection_id. 객체 데이터가 준비되면 검사60건이 조회된다.

**막힐 때 / 추가 설명:** inspection_id는 검사 한 건을 구분하고 batch_id는 어느 배치의 검사인지 알려준다. 두 키의 역할을 구분한다. Property ID나 API 이름이 자동 생성되면 기록하고, 이미 연결된 식별자를 임의로 바꾸지 않는다.

## 05. 배치에 검토용 Property 4개를 추가한다

**들어갈 곳:** 배치출하검토 → Properties → Create Property → Data

| 설정 항목 / 순서 | 입력하거나 확인할 값 |
| --- | --- |
| 검토 상태 | API 예시 reviewStatus / String / Description: 사람이 선택한 출하 검토 결과. 검토중, 보류, 승인 중 하나를 기록한다. |
| 보류 사유 | API 예시 holdReason / String / Description: 배치를 보류한 이유. 검토 상태가 보류일 때 반드시 입력한다. |
| 판정자 | API 예시 decidedBy / String / Description: 출하 검토 기록 액션을 실행한 사용자. |
| 판정 시각 | API 예시 decidedAt / Timestamp / Description: 출하 검토 기록 액션을 실행한 날짜와 시각. |
| 네 속성 공통 Data 설정 | Edit-only property. 원본 컬럼을 연결하지 않고 액션이 값을 기록하도록 설정 |
| 권한 기준 Dataset을 요구하면 | 배치 객체의 권한 기준 Dataset 확인·선택. 입력값을 읽어올 원천 컬럼 선택과 다른 설정 |

**저장:** 각 속성 추가 후 Save to branch. 기본4개 + 검토4개 = 총8개 확인.

**완료 확인:** 검토용 네 속성이 저장돼 있고 판정 시각만 Timestamp다. 기존 속성과 중복되지 않는다.

**막힐 때 / 추가 설명:** 표시 이름은 화면에서 읽는 이름, API 이름은 코드에서 구분하는 이름이다. 설명에 필수라고 적는 것만으로 입력이 강제되지는 않는다. 실제 필수 규칙은 액션에서 설정한다. 빈 검토 상태는 아직 기록되지 않은 상태이며 미검토라는 값이 자동 저장된 것은 아니다.

## 06. 배치와 검사를 Link Type으로 연결한다

**들어갈 곳:** Ontology Manager → New → Link type → Object type foreign keys

| 설정 항목 / 순서 | 입력하거나 확인할 값 |
| --- | --- |
| Many 쪽 Object Type | 품질검사 |
| Foreign key 속성 | 품질검사의 batch_id / Batch Id |
| One 쪽 Object Type | 배치출하검토 |
| 상대 기본키 | 배치출하검토의 batch_id / Batch Id |
| 관계 방향 | 배치1건 : 품질검사여러 건. 검사1건은 배치1건에 연결 |
| 양방향 이름 | 배치에서 보는 연결 목록: 품질검사 / 검사에서 보는 연결 대상: 배치출하검토 |
| API·설명 입력칸이 있으면 | 방향별 고유 API 이름 지정. 설명 예시: 배치별 품질검사를 batch_id로 연결해 검토 근거를 확인한다. |

**저장:** Link Type 생성 완료 → Save to branch.

**완료 확인:** 양쪽 타입과 키가 맞고, 데이터 준비 후 DEMO-01-02에서 내용량·점도2건이 연결된다.

**막힐 때 / 추가 설명:** 검사의 inspection_id를 배치 키에 연결하면 안 된다. inspection_id는 검사 자체의 키이고 배치를 찾는 값은 batch_id다. 타입 두 개가 같은 브랜치에 있어야 한다.

## 07. 출하 검토 기록 Action Type을 만든다

**들어갈 곳:** Ontology Manager → New → Action type

| 설정 항목 / 순서 | 입력하거나 확인할 값 |
| --- | --- |
| Display name | 출하 검토 기록 |
| API name 예시 | record-batch-review-practice-0909 · 입력칸의 유효성 검사와 이름 충돌 확인 |
| Description | 선택한 배치의 검토 상태와 보류 사유를 저장하고, 실행한 사용자와 시각을 함께 기록한다. |
| 규칙 종류 | Modify object · 기존 객체 수정. Create object로 새 배치를 만드는 흐름이 아님 |
| 수정할 Object Type | 현재 브랜치의 배치출하검토 |
| 대상 객체 Parameter | 배치출하검토 객체1개를 받는 파라미터 사용. 자동 생성됐으면 재사용 |
| 규칙의 대상 연결 | Modify 규칙이 위 대상 객체 파라미터를 참조하도록 연결. Parameter is missing 오류 없어야 함 |

**저장:** 생성 마법사를 완료하고 초안의 Rules·Parameters를 이어서 설정한다.

**완료 확인:** 수정 대상이 배치출하검토이고, 어느 객체를 수정할지 받는 대상 파라미터가 있다.

**막힐 때 / 추가 설명:** 자동 생성 액션과 New 방식은 화면 순서가 다를 수 있다. 입력 파라미터가 이미 있으면 같은 것을 두 번 만들지 않는다. Parameters가 안 보이면 우선 올바른 액션·브랜치와 대상 규칙이 구성됐는지 확인한다.

## 08. 사람이 입력할 Parameter를 설정한다

**들어갈 곳:** 출하 검토 기록 → Parameters

| 설정 항목 / 순서 | 입력하거나 확인할 값 |
| --- | --- |
| 대상 배치 | Object reference / 배치출하검토1개 / 필수. 설명: 검토 결과를 기록할 배치 |
| 검토 상태 | String / 선택지: 검토중, 보류, 승인 / Required 켜기 |
| 검토 상태 설명 | 검토 결과를 선택한다. 보류를 선택하면 보류 사유를 입력한다. |
| 보류 사유 | String / 기본 Required 끄기 / 사람이 이유를 적는 입력칸으로 표시 |
| 보류 사유 설명 | 내용량 불합격 등 배치를 보류하는 이유를 입력한다. |
| 판정자·판정 시각 | 사용자에게 입력받을 파라미터는 만들지 않음. Rules에서 자동 값 연결 |
| 미리보기 확인 | 검토 상태의 세 선택지와 보류 사유 입력칸이 보인다. 입력해야 할 칸이 숨김 처리돼 있지 않은지 확인 |

**저장:** Parameters 설정을 유지하고 다음 Rules 설정으로 이동.

**완료 확인:** 입력값은 대상 배치·검토 상태·보류 사유의3개. 불필요한 New parameter가 남지 않는다.

**막힐 때 / 추가 설명:** UI가 단일 선택을 제공하는 위치에서 선택지 세 개를 설정한다. Form/Layout이 별도로 있다면 해당 입력칸이 표시되는지 확인한다. 실제 표시 여부를 미리보기로 확인하는 것이 완료 기준이다.

## 09. 입력값과 저장할 속성을 연결한다

**들어갈 곳:** 출하 검토 기록 → Rules → Modify 배치출하검토

| 설정 항목 / 순서 | 입력하거나 확인할 값 |
| --- | --- |
| 수정 대상 | 8단계의 대상 배치 파라미터 |
| 검토 상태 속성 | 값의 출처: Parameter → 검토 상태 |
| 보류 사유 속성 | 값의 출처: Parameter → 보류 사유 |
| 판정자 속성 | 값의 출처: Current User |
| 판정 시각 속성 | 값의 출처: Current Time |
| Current Time이 안 보이면 | 배치 객체의 판정 시각 타입이 Timestamp인지 확인 → 타입·속성 저장 → 액션 규칙 다시 확인 |

**저장:** 규칙과 입력값 연결을 저장한다. 필수 오류가 있으면 다음 단계 전에 해결.

**완료 확인:** Modify 대상1개, 변경할 속성4개. 파라미터 누락이나 연결되지 않은 입력값이 없다.

**막힐 때 / 추가 설명:** 입력 파라미터는 액션이 전달받는 값이고 속성은 배치의 저장 칸이다. 보류를 선택하면 검토 상태 칸에 보류를 저장하도록 연결한다. 검사 결과와 측정값을 수정하는 규칙은 추가하지 않는다.

## 10. 보류일 때만 사유가 필수가 되게 한다

**들어갈 곳:** Parameters → 보류 사유 → General → Required

| 설정 항목 / 순서 | 입력하거나 확인할 값 |
| --- | --- |
| 1. 기본값 | Required 끄기 |
| 2. 예외 추가 | Add override → IF 영역의 condition |
| 3. 비교할 값 | Based on → Parameter → 검토 상태 |
| 4. 비교 조건 | Is equal to |
| 5. 비교 대상 | Compare against → Static value → String → 보류 |
| 6. 조건이 맞으면 | THEN → Is Required 켜기 |
| 7. 완료 | Done → Save to branch → 변경사항 확인 화면에서 Save to branch |

**저장:** Required 아래 1 override와 저장 완료 상태 확인.

**완료 확인:** 미리보기에서 검토중·승인은 필수 표시 없음, 보류에서만 사유 필수 표시.

**막힐 때 / 추가 설명:** 기본 Required까지 켜면 모든 상태에서 사유가 필수가 된다. 보류일 때만 켜려면 기본값을 끄고 override를 추가한다. 공백만 입력하는 경우의 처리와 중복 보류 금지는 별도 검증·조건이며 이번 완료 범위에 자동 포함되지 않는다.

## 11. 저장·조회·Test run을 확인한다

**들어갈 곳:** 브랜치 변경 목록 / 객체 데이터 상태 / 액션 Test run

| 설정 항목 / 순서 | 입력하거나 확인할 값 |
| --- | --- |
| 저장 확인 | 객체2개·링크1개·액션1개가 같은 브랜치에 저장됐는지 확인. 저장 오류 해결 |
| 객체 데이터 준비 | 현재 브랜치에서 배치와 검사 데이터 인덱싱이 완료됐는지 확인. 준비 요청이 표시되면 해당 브랜치의 두 타입 데이터 준비 진행 |
| 조회 확인 | 배치30건·검사60건. DEMO-01-02와 연결된 검사2건 조회 |
| 편집·권한 확인 | 배치 Edits Enabled. 현재 사용자에게 대상 객체 편집·액션 실행 권한이 있는지 확인. 기존 제출 조건과 권한을 임의로 삭제하지 않음 |
| Test run 입력 | 대상 DEMO-01-02 / 검토 상태 보류 / 사유 [실습] 내용량 검사 불합격으로 재검사 필요 |
| 변경 예정 값 확인 | 검토 상태·사유·판정자·판정 시각이 의도한 값인지 확인. Test run만으로 실제 저장 완료라고 판단하지 않음 |

**저장:** 모든 정의 저장과 데이터 준비를 완료한다. Test run 뒤 실제 제출은 다음 단계.

**완료 확인:** 액션이 선택한 배치의 검토 속성4개만 바꾸려고 하고, 실제 객체와 링크를 조회할 수 있다.

**막힐 때 / 추가 설명:** 타입 생성은 틀을 정의하는 일이고 인덱싱은 그 틀로 실제 데이터를 조회할 수 있게 준비하는 일이다. 0 objects이면 원천·출력 행, 브랜치, 인덱싱 상태를 차례로 본다. Unknown Resource가 보이면 객체 참조와 브랜치를 확인하고, 원인이 확인되지 않은 상태를 해결 완료라고 적지 않는다.

## 12. Insight에서 실제 제출하고 결과를 확인한다

**들어갈 곳:** 배치출하검토 → Overview → Open in → Insight

| 설정 항목 / 순서 | 입력하거나 확인할 값 |
| --- | --- |
| 1. 대상 열기 | 실습 브랜치 확인 → Title에서 DEMO-01-02 더블클릭 |
| 2. 검사 근거 확인 | Linked objects → 내용량 불합격98.20 / 점도 합격5100 확인 |
| 3. 액션 선택 | Actions → 출하 검토 기록 → 자동 지정된 배치 확인 |
| 4. 값 입력 | 검토 상태 보류 / 사유 [실습] 내용량 검사 불합격으로 재검사 필요 |
| 5. 실제 기록 | Submit → Edits successfully applied. 메시지 확인 |
| 6. 저장 결과 확인 | Properties에서 보류·입력한 사유·실행한 사용자·이번 실행 시각 확인. 다시 조회해도 값이 남는지 확인 |
| 7. 실습 완료 기록 | 브랜치·대상 배치·액션·결과·실행 시각 기록. 기존 성공 기록의실행 당시 시각는 과거 예시이며 현재 기대값이 아님 |

**저장:** Submit이 실제 기록 단계다. 저장 결과까지 확인한 뒤 실습을 마친다.

**완료 확인:** 배치에 검토 결과가 남고 기존 검사 측정값은 유지된다. 다음에 이어 할 때 사용할 브랜치와 마지막 성공 단계를 기록했다.

**막힐 때 / 추가 설명:** 검토 값은 다시 실행하면 갱신된다. 이 속성만으로 이전 결정의 전체 이력이 보존되지는 않는다. 기존 성공 브랜치는 review-practice였고 새 브랜치 재실습은 자신의 결과를 별도로 확인해야 한다.

## 마지막 누락 점검

- [ ] 모든 작업 탭의 프로젝트·브랜치 일치
- [ ] 원본 행 조회 / 검사 출력 빌드 성공
- [ ] 객체 이름·설명·데이터 소스·기본키·Title 확인
- [ ] 원천 속성 타입·컬럼 매핑 확인
- [ ] 검토용 속성4개 Edit-only / 판정 시각 Timestamp / 배치 Edits Enabled
- [ ] 링크의 Many=검사, One=배치, 외래키=batch_id
- [ ] 액션 대상 객체 / 입력 파라미터3개 / 속성 수정 규칙4개
- [ ] 보류일 때만 사유 필수
- [ ] 기존 실행 권한·제출 조건 확인 / 정의 저장 완료
- [ ] 해당 브랜치의 객체 데이터 준비 / 배치와 검사 연결 조회
- [ ] Test run의 변경 예정 값 확인
- [ ] 실제 Submit 후 상태·사유·판정자·시각 확인

근거: `practice-summary.md`, `presentation-notes.md`, 기존 브랜치 실습 기록. 이 문서는 새 브랜치에서 전 과정을 다시 실행했다는 완료 보고가 아니다.
