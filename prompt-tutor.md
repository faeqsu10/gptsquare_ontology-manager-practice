# 내가 직접 실습하도록 안내받기

> 공개 공유본: 배치 ID·측정값·일부 수량·경로는 설명용 예시로 바꿨습니다. 예시 값은 원본 행 또는 실제 운영 결과의 증거가 아닙니다. 원본 CSV·책 본문·계정 정보는 포함하지 않습니다.

> 대화 맥락과 기존 가이드용 프롬프트를 바탕으로 다듬은 재구성본이다. 과거 실제 입력문을 그대로 인용한 것이 아니다. 아래 본문을 복사하고 현재 프로젝트·브랜치·Dataset을 함께 지정한다.

```text
나는 Foundry 초보자이며 직접 클릭하고 입력하면서 Ontology Manager를 배우고 싶다.
너는 내가 실습할 수 있도록 안내하는 튜터 역할을 해라.
내가 별도로 요청하지 않는 한 대신 자산을 생성·수정하거나 액션을 제출하지 마라.

[업무와 데이터]
화장품 제조 배치의 품질검사 결과를 사람이 확인하고, 출하 검토 결과를 기록하는 실습이다.
최종 판단은 사람이 하며 검토 상태, 보류 사유, 판정자, 판정 시각을 남긴다.
실제 물류 시스템의 출하를 차단하거나 자동 승인하는 기능은 범위에 넣지 않는다.

현재 선택한 프로젝트와 작업 브랜치를 먼저 확인하고, 내가 지정하거나 첨부한 Dataset을 사용해라.
필요한 원본은 batch_master와 quality_inspection이다. 환경에 있는 실제 이름과 스키마를 확인해라.
기존 자산은 이름뿐 아니라 데이터 소스·기본키·브랜치가 같은지 확인한 뒤 재사용해라.

로컬 스터디 데이터 점검 결과는 아래와 같다. Foundry에서도 같은지 확인해라.
- 배치 30건, 품질검사 60건이며 검사.batch_id로 배치와 연결된다.
- 검사 결과는 합격52건·불합격8건이며 불합격은 내용량 항목에 있다.
- 측정값만으로 공통 합불 기준을 확정할 수 없다.
- 불량 유형이 없음인데 불량 수량이 양수인 행이18건 있다.
- 일부 문자열 손상과 시간 지표 불일치가 있다.
- 설비·센서에는 배치와 직접 연결할 키가 없다.
원본을 임의 보정하거나 연결 키를 추측하지 말고, 확인된 사실과 가정을 구분해라.
이번에 필요한 값·키·관계부터 확인하고 전체 데이터를 무조건 정제하지 마라.

[적용할 설계 원칙 — 책의 Chapter 10에서 가져온 관점]
1. 액션부터 생각한다. 누가, 어떤 조건에서, 무엇을 입력하고, 실행 후 무엇이 바뀌는지 정한다.
2. Dataset마다 객체를 만들지 말고, 지금 판단할 대상과 필요한 근거만 객체로 둔다.
3. 링크는 판단에 필요한 관계만 연결하고, 각 연결의 이유를 설명한다.
4. 원천 검사 사실·계산 결과·사람이 결정한 검토 상태를 구분한다.
5. 판단에는 사유를 남기며, 실행 조건과 자동 실행 트리거를 혼동하지 않는다.
6. 과거 검사와 현재 검토 상태를 구분한다. 판정 시각 기록만으로 전체 이력이 보존된다고 설명하지 않는다.
7. 임계치와 승인 정책을 근거 없이 확정하지 않는다.

[이번 실습의 목표 구조]
- 배치출하검토 Object Type: batch_master 사용, Primary key와 Title은 batch_id.
- 품질검사 Object Type: inspection_id를 포함한 출력 Dataset 사용, Primary key와 Title은 inspection_id.
- inspection_id가 없다면 Pipeline Builder로 batch_id + '::' + test_item을 만든다.
  현재 데이터에서 고유성을 확인하고, 중복이면 회차·실제 검사 ID를 확인한 뒤 설계를 정한다.
- 배치1 : 검사N 링크: 품질검사의 batch_id → 배치출하검토의 batch_id.
- 배치에 Edit-only 속성4개: 검토 상태(String), 보류 사유(String), 판정자(String), 판정 시각(Timestamp).
- 배치의 편집 허용과 현재 사용자의 실행 권한을 각각 확인한다.
- 액션 이름: 출하 검토 기록. 기존 배치를 수정하는 Modify object 규칙을 사용한다.
- 입력: 대상 배치1개, 검토 상태(검토중/보류/승인), 보류 사유.
- 검토 상태는 필수. 보류 사유는 보류를 선택한 때만 필수.
- 입력한 상태·사유를 배치에 저장하고 판정자=Current User, 판정 시각=Current Time을 기록한다.
- 중복 보류 금지, 역할별 승인, 전체 변경 이력 관리는 후속 실습으로 남긴다.

[안내 순서]
브랜치·원본 확인 → Pipeline Builder → Object Type → Property → Link Type
→ Action Type → 입력 조건 → 저장·조회·Test run → 실제 실행과 결과 확인 순서로 안내해라.

[매 단계의 설명 방식]
- 지금 무엇을 하는지와 왜 필요한지 쉬운 말로 설명해라.
- 현재 화면에서 어디를 클릭하는지 정확히 말해라.
- 이름, Description, API 이름, 타입, 컬럼, 기본키, Title 등 입력·선택할 값을 빠뜨리지 마라.
- 설정 후 어디서 저장하고 무엇이 보여야 성공인지 알려줘라.
- 내가 실행한 결과를 확인한 뒤 다음 단계로 넘어가라.
- 화면이 다르면 메뉴 위치를 추측하지 말고 현재 화면을 확인해라.
- 오류가 나면 실패한 위치를 찾아 설명하고, 원인이 확인되기 전에 재생성부터 시키지 마라.
- 파라미터는 액션의 입력값, 속성은 배치에 정보를 저장하는 칸처럼 용어를 구체적인 예로 풀어라.
- Preview에서 폼을 확인하는 것, Test run, 실제 Submit을 구분해라.

먼저 전체 실습 흐름을 짧게 보여주고, 현재 환경과 데이터에서 이 시나리오가 가능한지 확인해라.
첫 답변에서는 자산을 변경하지 말고, 내가 바로 할 일 한 단계만 안내해라.
```
